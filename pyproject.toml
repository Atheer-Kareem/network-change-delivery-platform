[project]
name = "network-change-delivery"
version = "0.1.0"
description = "A safety-focused reference platform for reviewed network-change delivery."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "ansible-core>=2.21.2,<2.22",
    "ansible-pylibssh>=1.4.0,<2",
    "ansible-runner>=2.4.3,<2.5",
    "httpx>=0.28.1,<0.29",
    "junos-eznc>=2.8.2,<2.9",
    "paramiko>=4,<5",
    "pydantic>=2,<3",
    "pybatfish>=2025.7.7.2423,<2025.8",
    "pyyaml>=6,<7",
]

[project.scripts]
ncdp = "network_change_delivery.cli:main"
ncdp-oxidized-service = "network_change_delivery.oxidized_reconciler:main"
ncdp-netbox-lab-service = "network_change_delivery.netbox_lab_reconciler:main"
ncdp-oxidized-observe = "network_change_delivery.oxidized_observation:main"
ncdp-oxidized-retire-trust = "network_change_delivery.oxidized_host_trust:retire_main"

[dependency-groups]
dev = [
    "ansible-lint>=26.6.0,<27",
    "pytest>=9.1.1,<10",
    "ruff>=0.16.2,<0.17",
]

[build-system]
requires = ["uv_build>=0.12.1,<0.13"]
build-backend = "uv_build"

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["A", "ARG", "B", "C4", "E", "F", "I", "N", "PERF", "PTH", "RUF", "SIM", "UP", "W"]

[tool.pytest.ini_options]
addopts = "--strict-config --strict-markers"
testpaths = ["tests"]
markers = ["batfish_integration: requires an externally running local Batfish service"]
